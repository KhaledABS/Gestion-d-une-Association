<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="{{path ('accueil')}}">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link{% if evenements is defined and evenements==evenements %} active{% endif %}" href="{{path('evenements')}}">Evénements </a>
      </li>
      
    </ul>
    <form class="form-inline my-2 my-lg-0">
    
      {# <ul class="navbar-nav mr-auto">

      {% if not app.user %} 
      <li class="nav-item"> <a href="{{ path('app_login')}}" class="nav-link">Connexion</a> </li>
      <li class="nav-item"> <a href="{{ path('app_register')}}" class="nav-link">Inscription</a> </li>
       {% else %} <li class="nav-item"> <a href="{{ path('app_logout')}}" class="nav-link">Déconnexion</a> </li> {% endif %}
       {% if is_granted('ROLE_ADMIN') %}  
      <li class="nav-item">
        <a  href="{{ path('easyadmin')}}"  class="nav-link">Admin</a>
        
      </li>
            <li class="nav-item">
        <a  href="{{ path('events_index')}}" class="nav-link">Proil</a>
        
      </li>
      {% endif %} 
    </ul> #}

    <ul class="navbar-nav ml-auto">
    {% if not app.user %} 
      <li class="nav-item"> <a href="{{ path('app_login')}}" class="nav-link">Connexion</a> </li>
      <li class="nav-item"> <a href="{{ path('app_register')}}" class="nav-link">Inscription</a> </li>
    {% endif %} 
    {% if is_granted('ROLE_ADMIN') %}  

       <li class="nav-item" > <a {# href="{{ path('easyadmin')}}" #} class="nav-link">Dhashboard</a> </li>

    {% endif %}
    </ul>
    <ul class="navbar-nav ml-auto">
    {% if is_granted('ROLE_USER') %}
          
					<li class="nav-item avatar dropdown">
        <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink-55" data-toggle="dropdown"
          aria-haspopup="true" aria-expanded="false">
          <img src="https://mdbootstrap.com/img/Photos/Avatars/avatar-2.jpg" class="md-avatar rounded-circle" alt="avatar image" style="height: 40px; width: 40px;">
        </a>
        <div class="dropdown-menu dropdown-menu-lg-right dropdown-secondary" aria-labelledby="navbarDropdownMenuLink-55">
           <a  class="dropdown-item"  href="{{ path('user_profile_index')}}" class="nav-link">Profil</a>
            
          <a class="dropdown-item" href="{{ path('app_logout')}}" class="nav-link">Déconnexion</a>
        {% endif %}
        </div>
      </li>
            
				</ul>
    </form>
  </div>
</nav>